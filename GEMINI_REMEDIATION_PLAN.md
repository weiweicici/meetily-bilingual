# Meetily Bilingual 修复执行计划

编制日期：2026-09-19
目标分支：审计所述 feature/live-translation（执行前核验实际分支）
依据：用户提供的技术与安全审计报告。本文是执行计划，不代表已经完成代码核验。

## 1. 授权范围与硬边界

- 仅修复当前 Tauri 桌面应用的实时翻译正确性、请求调度、翻译持久化、密钥保护、隐私告知及相关 UI 问题。
- 主应用为 frontend 下 Next.js/React/TypeScript 和 src-tauri 下 Rust；backend 为归档，禁止修改或新增 FastAPI 服务。
- 不改音频采集、混音、VAD、Whisper/Parakeet 推理行为；可只读追踪其事件契约。
- 不升级 Next.js、React、Tauri、Rust 主依赖版本，不进行无关重构，不更换翻译供应商。
- 优先复用现有数据库、凭据存储、设置、事件和测试设施。确需新依赖时先说明必要性、平台支持及替代方案；仅加入任务必需的最小依赖。
- 保留所有用户未提交改动；禁止 reset --hard、强制 checkout、清理工作区、删除模型或用户数据。
- 不自动提交、推送、创建 PR、发布、安装系统工具或修改全局环境变量。
- 默认不调用真实 Gemini API、不读取或打印真实 API Key、不发送会议内容。用 mock 完成测试；真实调用需用户另行明确授权，并使用合成文本。
- 不宣称未运行的测试已通过，不把环境构建失败归为代码验证通过。
- 用户要求暂停时立即停下；出现冲突、数据破坏风险或需要扩大范围时，只暂停受影响步骤并说明具体问题。

## 2. 执行规则

1. 先读取根目录及修改路径适用的 AGENTS.md，查看分支、git status 和现有 diff。
2. 按下列阶段顺序执行，每阶段实现、验证、更新进度后再进入下一阶段。正常步骤无需反复请求确认。
3. 在根目录新建 GEMINI_REMEDIATION_PROGRESS.md，记录各项：待核验/已确认/误报/已修复/验证受阻；附文件路径、证据、验证命令和结果。
4. 只读核验必须早于实现；以当前代码和官方资料为准，不能盲从审计示例。
5. 单阶段保持可审查的小范围 diff。发现既有错误要区分是否由本次修改引入；无关问题仅记录。
6. 不设固定 Token 承诺。不为节省用量跳过数据迁移或安全边界的必要验证。

## 3. 阶段 0：建立事实和验证基线

### 检查范围

定位实际文件，不假设报告中的路径一定存在：

- geminiTranslationService.ts、GeminiSettings.tsx。
- TranscriptContext.tsx、TranscriptPanel.tsx、TranscriptButtonGroup.tsx。
- VirtualizedTranscriptView.tsx、TranscriptSegment、useCopyOperations.ts。
- Tauri command 注册、数据库 schema/migrations、会议删除/备份/导入导出流程。
- CSP、Tauri capabilities、已有安全凭据存储、现有测试命令。

### 核验事项

- 查明 is_partial 的生产者、语义、事件顺序，以及最终片段是否还会修订。
- 查明 sequence_id 的作用域、会议切换时是否重置、与数据库 transcript id 如何关联。
- 追踪实时翻译和历史补译的所有入口、重试和持久化路径。
- 核验翻译表是否确实使用 localStorage、文本键是否用于兼容查找；不直接删除仍被读取的键。
- 核验已有错误处理：配额异常究竟导致丢数据、功能降级还是应用崩溃，据此修正严重度。
- 核验 memo/虚拟列表的 props 与测量行为，不根据组件名称推断只重绘单条。
- 执行项目已有的相关类型检查、测试或构建基线；避免无必要的全量昂贵构建。

### 外部信息核验

通过 Google 官方文档核验当前模型标识、生命周期、generateContent 支持和限流规则，记录网址及访问日期。报告中的 1.5/2.0 或 3.x 名称均不能直接采信。限额按项目、模型、付费层级和服务响应处理，不能写死“免费版 15 RPM”。未经授权不用用户密钥查询模型列表。

### 验收

形成逐项核验表及最小改动清单。若当前分支与审计不同但目标代码存在，可继续核验；目标代码缺失或需切换含未提交修改的分支时停止相关实现并报告。

## 4. 阶段 1：转录与译文对应正确性

### 实现

- 仅对契约确认的最终文本发起翻译；依据真实事件类型判断，不能仅机械加入 !is_partial。
- 翻译任务身份包含会议/录制会话和稳定片段标识；若文本可修订，再加入文本版本或指纹。
- 相同版本去重；新版本不可被旧版本的进行中锁永久阻止。
- 旧响应返回时验证会话和文本版本，禁止覆盖新译文或写到另一场会议。
- 成功、失败、取消均释放进行中状态；失败可按策略重新提交。
- 明确停止录制与切换会议的差异：正常结束时按现有产品流程完成最终片段和补译；切换、删除或撤销翻译授权时取消/隔离旧任务。
- 清理事件监听和任务引用，避免重复订阅。

### 验收测试

- partial → final：只翻译最终文本。
- 相同 final 重复到达：同版本仅请求一次。
- 旧文本请求慢、新文本请求快：旧结果不覆盖新结果。
- 切换会议后旧响应到达：不污染新会议。
- sequence_id 在两场会议重复：译文互不串用。
- 失败后可恢复，停止录制后的最后一句不遗漏。

## 5. 阶段 2：统一请求调度及模型错误处理

### 实现

- 实时和历史补译共用一个调度入口，避免各自队列叠加突破限额。
- 默认并发 1；只有充分理由时设 2。报告“12”不可理解为并发 12。
- 并发限制与每分钟请求预算分别控制；350ms 间隔不等于满足 RPM 限额。
- 支持可配置请求预算、平滑调度、任务去重、有界队列、超时及 AbortController 取消。
- 队列满或达到重试上限时保留“待补译/失败”状态与用户可见提示，不静默丢弃。避免内存无限增长。
- 429 优先遵守可用 Retry-After/服务重试提示，采用有上限指数退避和抖动，并让共享调度器冷却。
- 网络错误及部分 5xx 有界重试；401/403 停止自动重试并提示配置/权限问题；400 按错误原因分类，不盲目重试。
- 仅在确认模型不存在、不支持或已下线时进行模型回退，并缓存本会话不可用状态；不每条字幕重复撞同一个无效模型。
- 429 不通过轮换模型无限绕过配额；模型可用性和配额耗尽分别处理。
- 译文为空、异常格式或不可用时不标记成功，不把错误响应写成译文。
- 配置变更/清除密钥/关闭翻译后停止新请求，处理排队和在途任务。

### 验收测试

使用 mock 和假时钟验证：突发 20 条任务时最大并发和预算符合配置；429 冷却后恢复；404 回退有界且缓存生效；401/403 无重试风暴；超时/取消不死锁；实时与补译共同受控；队列饱和可恢复。

## 6. 阶段 3：SQLite 持久化与旧数据迁移

### 设计约束

- SQLite 为持久化首选，复用现有 Rust 数据层和迁移机制。不要再新增一套平行存储服务。
- 根据现有 schema 选择 translation 列或独立翻译表。仅单语言单版本且关系简单时可用列；需要状态、版本或多语言时用表。把选择理由写入进度文件。
- 持久化关联使用稳定 transcript id 和 meeting id；不要假设运行期 sequence_id 永久唯一。
- 记录源文本版本/指纹及目标语言等必要元数据，防止修改原文后复用旧译文。
- 若录制时 transcript 尚未入库，设计会话暂存与最终 id 对账；最终入库前不得提前丢弃译文。

### 实现

- 添加兼容现有数据库的版本化迁移，以及受会议范围约束的查询、批量 upsert 和删除逻辑。
- 翻译完成后增量保存，避免每条字幕全量序列化整场会议。
- 写入失败需明确反馈并允许重试，不把“显示在内存里”当成持久化成功。
- 旧 localStorage 迁移必须幂等：解析 → 验证会议/片段映射 → 事务写入 → 校验提交结果 → 标记迁移完成。
- 原始数据只在确认成功持久化后清理；失败保留。文本键存在歧义、无法映射或 JSON 损坏时，不猜测归属、不删除源数据，报告待处理数量。
- 删除会议时连带删除译文；接入现有备份和恢复、双语复制/导出流程，不擅自重写整个备份系统。
- 过渡期间兼容读取旧数据，但不能长期双写造成两份事实来源。

### 验收测试

- 新建/历史会议保存、重启恢复、双语复制导出、删除无残留。
- 空库、已有库升级成功；跨会议同 sequence_id 不串数据。
- 迁移执行两次不重复；部分失败重跑可恢复；损坏数据保留。
- 重复文本片段、无法映射项不错误关联。
- 模拟数据库写失败/中断：不清理旧数据，不虚报保存成功。
- 使用数千条合成片段验证增量写入与读取，无 localStorage 配额依赖。

## 7. 阶段 4：密钥存储与明确云端授权

### 实现

- 先复用项目已有凭据方案；否则选支持目标平台的系统凭据库或合理配置的 Stronghold。不得把加密密钥/口令硬编码进源码或与密文一起明文保存。
- Gemini 请求优先由 Rust 发起，使前端仅得到“已配置/未配置”状态；保存时接收用户输入，后续调用不把密钥回传前端。
- 如将 HTTP 请求迁到 Rust，保持阶段 2 的统一调度语义，避免重复队列。command 不接受任意 URL，不成为任意网络代理；目标 HTTPS 域名与请求结构受约束。
- 旧密钥迁移：成功写入安全存储并验证可读取后，删除对应 localStorage 值；失败保留并提示。禁止日志输出密钥或其完整请求头。
- 保存密钥和允许上传会议文本分开表达：默认关闭云翻译，由明确开关/确认动作启用；已有用户升级后也不能未经告知自动上传。
- 提示内容明确：本地转录文本将发往 Google Gemini API；本次翻译不需要上传原始音频；数据处理受所用服务条款约束。
- 清除密钥或关闭翻译立即阻止新请求并取消可取消任务；说明已发送请求无法撤回。
- 检查 CSP 和 capabilities，只收紧确属本功能的权限，不破坏其他供应商现有功能。

### 验收测试

- 首次使用无授权时零网络请求；仅保存密钥仍不自动上传。
- 开启/关闭/清除状态即时生效，重启后状态正确。
- 前端持久化和日志中不再有新保存的明文密钥。
- 安全存储失败有可操作提示，不静默退回明文。
- 迁移失败不丢失现有密钥；迁移成功清理旧值。
- 真实凭据和真实会议内容不进入测试快照、进度文件、错误日志。

## 8. 阶段 5：界面与性能收尾

- GeminiSettings 复用项目主题 token/组件风格，支持浅色和深色、错误、禁用及加载状态。
- 展示必要的等待、重试、失败和补译进度；用语面向用户，避免暴露内部错误堆栈。
- 审查虚拟列表动态测量机制，使译文出现或换行后高度更新；仅修改 estimateSize 不算完整解决。
- 测试长文本、快速滚动和自动跟随；用户向上查看历史时不强制拉回底部。
- 移除或降级本功能的冗余调试日志，禁止输出会议文本和密钥；保留必要的非敏感错误诊断。Emoji 本身不作为清理依据。
- 保持现有时间戳、英文原文与中文译文的复制/导出格式。

## 9. 最终验证与交付

1. 执行与修改有关的现有前端类型检查、lint、单元测试及构建；命令以 package.json 为准。
2. 执行相关 Rust 格式检查、测试与可行的 cargo check；避免为全仓库格式化引入无关 diff。
3. Windows 如缺少 libclang，先只读检查现有工具链和项目构建脚本。不得自行安装 LLVM/VS 或修改系统环境；记录阻塞及建议命令。已安装路径可按当前进程设置使用。
4. 通过 mock 覆盖正常翻译、限流、模型故障、离线、取消、会议切换、重启、旧数据迁移和保存失败。
5. 真机录音、真实 Gemini 请求及其他平台验证如未获授权或环境不可用，明确标为未验证；不以 Windows 结果宣称 macOS/Linux 已通过。
6. 检查最终 diff，确认未改归档 backend、音频核心及无关文件，未引入密钥或会议数据。
7. 更新进度文件，交付：变更摘要、文件列表、实际验证结果、剩余问题、数据迁移注意事项。

## 10. 完成标准

- 最终原文与对应译文一致，跨会话和响应乱序不污染数据。
- 请求负载可控，重试有界，失败可恢复。
- 翻译可靠持久化，旧数据不因迁移失败丢失。
- 密钥安全存储，云端翻译有明确授权。
- 原有录制、转录、查看会议和复制导出流程未被破坏。
- 所有已实施修改都有相称的验证证据；任何阻塞或未验证项清楚列明。
